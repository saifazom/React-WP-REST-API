version: '1'
app:
  name: react-starter-kit
  type: web
  repository: react-starter-kit  
  
build:
  dockerfile: Dockerfile
  context: .

deploy:
  replicas: 1
  port: 3000
  stages:
    - name: production
      branch: main
      auto_deploy: true
      environment:
        NODE_ENV: production