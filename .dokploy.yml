version: '1'
app:
  name: Frontend/coffeeclub-frontend-bqvwxu
  type: web
  
build:
  dockerfile: Dockerfile
  context: .

deploy:
  replicas: 1
  port: 3000
  stages:
    - name: production
      branch: main
      auto_deploy: true
      environment:
        NODE_ENV: production
